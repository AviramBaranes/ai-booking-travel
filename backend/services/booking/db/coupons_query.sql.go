// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: coupons_query.sql

package db

import (
	"context"
)

const createCoupon = `-- name: CreateCoupon :one
INSERT INTO coupons (name, code, discount, is_enabled, created_at, updated_at)
VALUES ($1, $2, $3, $4, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
RETURNING id, name, code, discount, is_enabled, created_at, updated_at
`

type CreateCouponParams struct {
	Name      string
	Code      string
	Discount  int32
	IsEnabled bool
}

func (q *Queries) CreateCoupon(ctx context.Context, arg CreateCouponParams) (Coupon, error) {
	row := q.db.QueryRow(ctx, createCoupon,
		arg.Name,
		arg.Code,
		arg.Discount,
		arg.IsEnabled,
	)
	var i Coupon
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Code,
		&i.Discount,
		&i.IsEnabled,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteCoupon = `-- name: DeleteCoupon :exec
DELETE FROM coupons
WHERE id = $1
`

func (q *Queries) DeleteCoupon(ctx context.Context, id int32) error {
	_, err := q.db.Exec(ctx, deleteCoupon, id)
	return err
}

const findCouponByCode = `-- name: FindCouponByCode :one
SELECT id, name, code, discount, is_enabled, created_at, updated_at
FROM coupons
WHERE code = $1
`

func (q *Queries) FindCouponByCode(ctx context.Context, code string) (Coupon, error) {
	row := q.db.QueryRow(ctx, findCouponByCode, code)
	var i Coupon
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Code,
		&i.Discount,
		&i.IsEnabled,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listCoupons = `-- name: ListCoupons :many
SELECT id, name, code, discount, is_enabled, created_at, updated_at
FROM coupons
ORDER BY created_at DESC
`

func (q *Queries) ListCoupons(ctx context.Context) ([]Coupon, error) {
	rows, err := q.db.Query(ctx, listCoupons)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Coupon
	for rows.Next() {
		var i Coupon
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Code,
			&i.Discount,
			&i.IsEnabled,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateCoupon = `-- name: UpdateCoupon :one
UPDATE coupons
SET name       = COALESCE($1, name),
    code       = COALESCE($2, code),
    discount   = COALESCE($3, discount),
    is_enabled = COALESCE($4, is_enabled),
    updated_at = CURRENT_TIMESTAMP
WHERE id = $5
RETURNING id, name, code, discount, is_enabled, created_at, updated_at
`

type UpdateCouponParams struct {
	Name      *string
	Code      *string
	Discount  *int32
	IsEnabled *bool
	ID        int32
}

func (q *Queries) UpdateCoupon(ctx context.Context, arg UpdateCouponParams) (Coupon, error) {
	row := q.db.QueryRow(ctx, updateCoupon,
		arg.Name,
		arg.Code,
		arg.Discount,
		arg.IsEnabled,
		arg.ID,
	)
	var i Coupon
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Code,
		&i.Discount,
		&i.IsEnabled,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
